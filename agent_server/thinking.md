**目標：**\n*   建立一個專屬於自己的大型語言模型（LLM）應用。\n*   開發一個能夠與現有前端（client.md）對接的伺服器端。\n\n**伺服器端（Server-side）核心功能：**\n\n1.  **基本架構：**\n    *   以現有的 `server.py` 作為基礎進行擴充。\n    *   伺服器端與前端之間的通訊將是純文字傳輸，類似於一般的大型語言模型 API（文字傳入，文字傳出）。\n\n2.  **不考慮串流功能：**\n    *   初期階段不考慮實作串流（streaming）功能，暫時略過。\n\n3.  **整合現有 AI 代理人能力：**\n    *   **目的：** 不與現有開源 AI 代理人（如 `aider`）競爭市場，而是透過整合它們的能力來打造個人化的應用。\n    *   **可整合的 AI 代理人範例：**\n        *   `aider`\n        *   `gemini cli`\n        *   `claude agent`\n        *   其他類似的 AI 代理人\n    *   **整合流程（設想）：**\n        *   伺服器端會先進行一些初步的處理。\n        *   將處理後的檔案丟到指定的資料夾。\n        *   調用（call）這些 AI 代理人的功能來執行任務。\n\n4.  **授權考量：**\n    *   **伺服器端不考慮授權問題。** 您表示完全信任伺服器端，因此不納入授權驗證的考量。\n\n**總結：**\n\n您希望在現有的 `server.py` 基礎上，開發一個能夠與前端（`client.md`）進行文字交流的伺服器端。這個伺服器端的核心價值在於整合多個現有的 AI 代理人（如 `aider`、`gemini cli`、`claude agent` 等）的能力，以實現個人化的 LLM 應用，而非建立一個獨立的 AI 代理人。在初期開發階段，將不考慮串流功能，並且伺服器端本身不涉及授權的驗證。",
