# 氣候生成、載具物理與地質侵蝕深度分析 (Analysis)

## 1. 氣候變量的數學模型 (`world/src/sim/mod.rs`)
Veloren 的氣候分佈是由多維地理參數加權生成的。

### 核心公式標註：
- **溫度負相關海拔**：[Line 2563] `1.0 - alt_uniform` 被賦予最高權重 (2.0)。這保證了高山雪地與平原溫熱的物理規律。
- **動態蒸發機制**：[Line 2576] 濕度計算引入了溫度補償項 `(temp - CONFIG.tropical_temp)`。熱帶地區雖然降雨多，但水分蒸發也快，這影響了 `Jungle` 與 `Savannah` 的邊界判定。

## 2. 移動平台的浮力與流體力學 (`common/systems/src/phys/mod.rs`)
統一的力學框架支援了載具與生物的自然交互。

### 關鍵機制：
- **廣義浮力公式**：[Line 116] 利用實體密度與介質密度的差值動態修正 Z 軸加速度。
- **流體相對速度補償**：[Line 85] `vel.0 - fvel.0`。所有的物理計算（阻力、升力）均基於實體與環境流場的相對速度，這確保了在移動的船隻或強風中，戰鬥判定的幾何一致性。

## 3. 地質演進：非線性侵蝕算法 (`world/src/sim/erosion.rs`)
河流與山脈的形成源於精密的物理模擬。

### 核心代碼標註：
- **數值穩定性求解**：[Line 1320] 使用 Newton-Raphson 迭代法確保侵蝕方程在極端地形斜率下依然收斂，避免產生浮點數崩潰。
- **河流路徑自適應**：[Line 1430] 透過計算沉積物 (`sediment`) 與岩床高度的差值，河流會「自動」尋找最易侵蝕的路徑前進，形成分叉與匯流的自然地貌。

---
*本文件由分析任務自動生成預定留檔。*
