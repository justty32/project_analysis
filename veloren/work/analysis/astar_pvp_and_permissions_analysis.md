# A* 尋路優化、PVP 模式與權限系統深度分析 (Analysis)

## 1. A* 尋路成本與路徑回溯 (`common/src/astar.rs`)
Veloren 的尋路核心是一個高度優化的泛型搜尋器。

### 核心代碼標註：
- **路徑成本公式**：[Line 220] `cost_estimate = cost + h`。其中 `cost` 是從起點到當前點的累積代價，`h` 是啟發式函數（通常是距離估算）。
- **非阻塞計算**：[Line 245] 搜尋過程被限制在 `max_iters` 內。如果單次 Tick 無法算完，會返回 `PathResult::Pending`，這保證了大規模 NPC 尋路時不會凍結伺服器。
- **路徑還原**：[Line 265] `reconstruct_path_to` 函數透過 `came_from` 鏈條回溯，實現了高效的內存複用與路徑產出。

## 2. PVP 判定與戰鬥模式切換
PVP 的合法性是基於事件驅動與陣營矩陣的交集。

### 關鍵機制：
- **戰鬥模式事件**：透過 `SetBattleModeEvent` [Line 289, common/src/event.rs] 切換玩家的 PVP 標記。
- **動態敵對邏輯**：系統會實時檢查雙方的戰鬥狀態。只有當雙方均同意或處於特定 PVP 區域時，`hostile_towards` 才會解鎖玩家間的傷害傳遞。

## 3. 建築保護與管理權限 (`server/src/cmd.rs`)
確保世界結構安全的核心校驗層。

### 核心流程：
- **權限賦予**：[Line 265] 管理員權限透過 UUID 與特定組件 (`comp::Admin`) 綁定。
- **區域權限校驗**：當發送地形更新請求時，伺服器會進行坐標與 `Site` 範圍的碰撞檢測。若坐標處於受保護的城鎮內且發送者無權限，該請求將被靜默捨棄。
- **高級指令保護**：[Line 5798] 展示了複雜數據（如 Buff 或地形變更）在生成前必須經過權限過濾器 (`build_buff`) 的處理。

---
*本文件由分析任務自動生成預定留檔。*
