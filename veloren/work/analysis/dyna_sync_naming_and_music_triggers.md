# 載具同步、程序化命名與音軌觸發深度分析 (Analysis)

## 1. 移動平台的座標矩陣與安全脫離 (`common/src/mounting.rs`)
Veloren 透過局部矩陣變換實現載具上的穩定物理。

### 核心代碼標註：
- **載具矩陣計算 (`get_mount_mat`)**：[Line 300] 系統動態構建 4x4 矩陣，將載具的位移、三軸旋轉與縮放封裝。實體的物理位置在此矩陣下進行局部更新，實現了「船在動、人在船上走」的穩定感。
- **下船安全校驗**：[Line 168] 預防性空間檢查。當實體脫離載具時，系統執行 `is_space` 與 `find_ground` 邏輯，確保玩家不會因載具高速運動而掉入體素裂縫或卡在垂直牆面。

## 2. 基於音節與語意的程序化命名 (`world/src/site/namegen.rs`)
名稱生成器兼顧了語言規律與隨機多樣性。

### 關鍵機制：
- **結構化音節組合**：[Line 42] `generate` 函數。透過 `start` + `(vowel + middle) * n` + `end` 的模式，模擬了真實語言的構詞規律。
- **環境語意映射**：[Line 145] 對於特定地形（如 Grassland），系統放棄純隨機音節，改用具有環境意象的詞綴池（如 "mistral", "whisper", "shimmer"），提升了地圖生成的文化沉浸感。

## 3. 音軌管理器與活動感知 (`voxygen/src/audio/music.rs`)
動態 BGM 系統是營造戰鬥壓力的核心。

### 核心流程：
- **活動狀態輪詢 (`maintain`)**：[Line 233] 音樂管理器實時監控 `MusicActivity`。
- **無縫轉場邏輯**：[Line 217] 透過 `is_gap` 與 `gap_time` 參數，系統能精確控制曲目切換時的空白期，防止戰鬥音樂突然中斷或重疊，保證了聽覺體驗的連貫性。

## 4. 水文邊界判定：河流入海邏輯 (`world/src/sim/mod.rs`)
### 核心代碼標註：
- **入海口判定**：[Line 1571] 透過 `dh[lake_idx] < 0` 判定地勢跌落至最低邊界。系統將此類節點強制同步至海平面高度 (0.0)，實現了河流與大洋的平滑匯流。

---
*本文件由分析任務自動生成預定留檔。*
