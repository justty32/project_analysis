# 3D 音訊、地城難度與 RTSim 規則深度分析 (Analysis)

## 1. 3D 音訊空間定位架構 (`voxygen/src/audio/mod.rs`)
Voxygen 透過監聽器模型實現沈浸式聽覺體驗。

### 核心代碼標註：
- **監聽器同步**：[Line 245] `ListenerInstance` 封裝了 `kira` 句柄與 3D 座標。系統每幀將攝像機的物理位置同步至此，作為所有 3D 音效的參考點。
- **分層濾波控制**：[Line 240] 每個音軌（SFX, Ambience）都具備獨立的 `FilterBuilder`。這支持了如「水下悶響」或「地洞回聲」的局部濾波效果。
- **動態頻道分配**：[Line 285] SFX 頻道在創建時即綁定監聽器 ID，確保了爆炸、技能等聲音具備正確的距離衰減與左右聲道平衡。

## 2. 地城難度與層級階梯邏輯
地城的挑戰性是由地理深度與種族元數據共同定義的。

### 關鍵機制：
- **層級難度權重**：地城生成器（如 `AdletStronghold`）會接收一個 `depth` 參數。每一層地下空間都會增加怪物的等級偏移量 (`level_offset`)。
- **精英怪觸發**：Boss 房與寶箱房被標記為特殊的 `Plot`，其內部的怪物生成器會調用高階 `NpcBuilder` 模板，並賦予額外的 `Poise` (霸體) 屬性。

## 3. RTSim 社會規則與人口模擬 (`rtsim/src/rule/`)
Veloren 的世界具備長期的社會演化邏輯。

### 核心規則：
- **人口遷移 (`migrate.rs`)**：系統根據城鎮的「繁榮度」與「安全評分」計算人口流動向量。這直接影響了城鎮的生產力。
- **NPC 行為模擬 (`simulate_npcs.rs`)**：[Line 11962 bytes] 處理 NPC 的宏觀意圖（如：去礦區採礦、回城交易）。這些意圖最終轉化為 `server/agent` 的微觀行動路徑。

---
*本文件由分析任務自動生成預定留檔。*
