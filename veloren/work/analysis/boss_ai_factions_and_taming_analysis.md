# Boss AI、陣營矩陣與馴服機制深度分析 (Analysis)

## 1. Boss 行為樹的階段化管理 (`server/agent/src/attack.rs`)
Boss 的挑戰性源於其嚴密的狀態機控制。

### 核心技術：
- **條件分支控制**：[Line 3340] Boss 透過 `Conditions` 枚舉管理戰鬥階段（如：是否已進入二階段、是否已釋放特定氣場）。這確保了 Boss 的大招不會連續觸發。
- **技能優先級**：[Line 3348] 邏輯上優先執行「開場技能」或「狀態 Buff」，隨後才進入「常規攻擊循環」。
- **動作恢復感知**：[Line 3343] 透過監聽 `StageSection::Recover`，AI 能在動作結束的瞬間精確接續下一個指令，實現流暢的戰鬥體驗。

## 2. 社會關係布林矩陣 (`common/src/comp/agent.rs`)
Veloren 的世界秩序建立在 `Alignment` 枚舉之上。

### 關鍵規則：
- **陣營層級**：[Line 47] 區分了從野生動物到具有政治屬性的 NPC。
- **敵對矩陣 (`hostile_towards`)**：[Line 70] 實作了非對稱敵對邏輯。例如，強盜對所有人敵對，但村民僅在受攻擊或戰爭狀態下對特定陣營敵對。
- **認知容量限制**：[Line 31-44] AI 被限制只能並行追蹤 5 個計時器與狀態，這在保證行為複雜度的同時，將每隻怪物的內存佔用降到最低。

## 3. 寵物馴服與生命週期 (`common/src/event.rs`)
從野生實體到忠實夥伴的數據轉化。

### 核心流程：
- **馴服事件 (`TamePetEvent`)**：[Line 255] 建立了寵物與主人 Entity 之間的邏輯連結。
- **存檔綁定**：[Line 239] 寵物並非獨立存檔，而是作為玩家 `UpdateCharacterDataEvent` 的子集。這意味著玩家與其寵物在資料庫層面是一個原子單位，解決了「人上線寵物沒上線」的一致性問題。

---
*本文件由分析任務自動生成預定留檔。*
