# 戰鬥射線、文明擴張與道路幾何深度分析 (Analysis)

## 1. 嚴密的戰鬥牆壁遮擋算法 (`common/systems/src/melee.rs`)
Veloren 的命中判定設計考慮了極限狀態下的公平性。

### 核心代碼標註：
- **九射線掃描**：[Line 345] 系統不依賴單一射線，而是從發起者 Aabb 與目標 Aabb 的 8 個頂點加中心點建立連線。只有當至少一條射線穿透且未撞擊 `is_filled()` 體素時，才判定為未被遮擋。這有效防止了「隔牆空揮」的漏洞。
- **動態坐標投影**：[Line 375] `projected_point` 確保了即使雙方在高速運動的載具上，只要幾何射線在數學上與目標體積相交，命中就會被承認。

## 2. 文明擴張的經濟代價模型 (`world/src/civ/mod.rs`)
地圖上的權力版圖是由導航成本驅動的。

### 關鍵機制：
- **A* 成本繼承**：[Line 839] `route_between` 利用預生成的 `tracks` 成本進行路徑規劃。這模擬了地理環境（如山脈、河流）對文明發展範圍的物理限制。
- **高效能確定性雜湊**：[Line 846] 使用 `FxHasher64` 處理 A* 節點，權衡了在世界生成階段數萬次尋路運算的極速需求與確定性輸出。

## 3. 道路的程序化幾何與視覺美化 (`world/src/sim/way.rs`)
道路系統結合了物理空間與藝術感的平衡。

### 核心公式標註：
- **弧形淨空公式**：[Line 45] `head_space = 8 - dist^6`。這是一個非線性的高度衰減模型，保證了程序化生成的隧道頂部是自然的圓拱形而非直角。
- **自適應色彩映射**：[Line 48] 道路顏色並非固定，而是基於底層生物群系的顏色進行 0.7 倍的亮度衰減與隨機噪聲處理，實現了道路與環境的完美視覺融合。

---
*本文件由分析任務自動生成預定留檔。*
