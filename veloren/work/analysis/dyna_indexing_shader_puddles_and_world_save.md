# 載具索引優化、Shader 積水效應與世界存檔深度分析 (Analysis)

## 1. 移動載具的欄式記憶體佈局 (`common/src/volumes/dyna.rs`)
飛船與載具在 Veloren 中透過高效的索引映射實現物理一致性。

### 核心代碼標註：
- **欄式存取優化 (`ColumnAccess`)**：[Line 130] 透過 `x * sz.y * sz.z + y * sz.z + z` 的公式，系統將 Z 軸資料排布在連續內存。這對於物理引擎頻繁進行的地面接觸判定（垂直射線檢測）極其友善。
- **高效座標邊界檢查**：[Line 38] `idx_for` 函數封裝了極速的 Aabb 包圍盒檢測，確保了實體與載具間的座標投影不會產生非法內存訪問。

## 2. 動態雨天積水與漣漪 Shader (`assets/voxygen/shaders/terrain-frag.glsl`)
Shader 如何模擬真實的降雨環境感。

### 關鍵機制：
- **噪聲積水映射 (`puddle`)**：[Line 261] 透過 `noise_2d` 與 `f_sky_exposure` 的交集判定，系統能精確識別哪些地面被雨水淋濕，並動態賦予流體材質屬性。
- **表面法線擾動**：[Line 301] 當雨滴觸碰積水區時，Shader 會即時修改頂點法線向量 (`f_norm.xy`)。這種純 Shader 端的模擬避免了 CPU 處理數萬顆雨滴碰撞的開銷，同時保證了視覺上的物理正確感。

## 3. 世界地圖的持久化與版本遷移 (`world/src/sim/mod.rs`)
Veloren 的世界數據採用結構化的高度圖序列化。

### 核心流程：
- **二進位序列化 (`bincode`)**：[Line 345] 遊戲使用 `bincode` 進行地圖數據的高速讀寫，這比 RON 格式更適合處理數百 MB 的地形數據。
- **一致性保護**：[Line 365] 系統會校驗地圖的 `GenOpts`。若種子 (`seed`) 或縮放比例 (`scale`) 發生變動，會強制觸發地圖重建，防止產生斷裂的程序化地形。

---
*本文件由分析任務自動生成預定留檔。*
