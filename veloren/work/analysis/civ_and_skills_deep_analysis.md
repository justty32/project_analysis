# 文明發展與技能體系深度分析 (Analysis)

## 1. 文明生成與環境自適應 (`world/src/civ/mod.rs`)
Veloren 的城鎮生成展現了極高的地理感知能力。

### 核心技術：
- **地形動態修正**：在放置建築前，生成器會執行「螺旋平整算法」，將原本凹凸不平的地形過渡為建築平面，同時保護水體邊界。
- **類型化生成器**：支援 20 多種 `SiteKind`。不同地形（沙漠、叢林、海岸）會觸發完全不同的城市佈局邏輯。
- **社會連通性**：城鎮並非孤島。系統會自動計算最近鄰居，並根據 A* 路徑長度計算貿易成本，這直接餵給了 RTSim 經濟模型。

## 2. 技能樹與數值平衡架構 (`common/src/comp/skillset/skills.rs`)
RPG 要素的核心在於嚴密的技能依賴與係數管理。

### 核心機制：
- **枚舉化定義**：將所有主動與被動技能嚴格類型化，避免了字串解析的開銷與錯誤。
- **靜態數值橋樑 (`SkillTreeModifiers`)**：這是一個設計上的亮點。它將「遊戲平衡參數」與「程序執行邏輯」分離。系統只需要讀取這些靜態係數，就能在全域範圍內套用技能加成（如傷害、速度、冷卻）。
- **進階動作解鎖**：部分技能不僅是數值提升，還涉及新動作狀態（如 `UnlockShotgun`）的開關，這直接影響了玩家狀態機的合法切換路徑。

---
*本文件由分析任務自動生成預定留檔。*
