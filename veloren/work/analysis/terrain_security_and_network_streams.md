# 地形修改安全性、地城連通與網路流優先級深度分析 (Analysis)

## 1. 伺服器地形修改的權限校驗 (`server/src/sys/terrain.rs`)
確保世界結構不被非法篡改的防禦層。

### 核心代碼標註：
- **持久化變更校驗**：[Line 152] 在將生成的區塊發送給客戶端前，系統會強制調用 `terrain_persistence.apply_changes`。此處集成了區域權限判定，若請求坐標位於受保護的 Site 內，變更將被攔截。
- **實體同步補償**：[Line 174] 區塊更新不僅包含體素，還包含「Supplement」實體。這保證了當玩家建造建築時，對應的 NPC 生態能即時適應新的地形。

## 2. 地城房間連通性實作 (`world/src/site/tile.rs`)
地城的拓撲連通性是透過精確的 Tile 標記實現的。

### 關鍵機制：
- **強地地塊 (`AdletStronghold`)**：[Line 194] 系統為不同類型的地城定義了專屬的 `TileKind`。這些標記引導幾何原語在 3D 空間中進行 `Union` (聯集) 運算，確保房間與通道之間沒有斷裂。
- **邊界保護**：[Line 252] `AdletStronghold` 被標記為障礙物，這在世界生成的巨集觀層面就排除了其他隨機生成的干擾（如樹木穿牆）。

## 3. 網路封包的隔離與優先級調度 (`server/src/client.rs`)
為了解決高頻數據下的網路擁塞問題。

### 核心技術：
- **獨立地形流 (Stream 5)**：[Line 164] 將巨大的 `TerrainChunkUpdate` 訊息與戰鬥訊息隔離。這保證了玩家在切換地圖塊時，不會因為加載地圖而導致戰鬥卡頓。
- **高頻同步流 (Stream 3)**：[Line 229] 專用於 `EntitySync` 與 `CompSync`。這是遊戲流暢感的命脈，具備最高的處理優先權。
- **交易原子性保證**：[Line 199] `UpdatePendingTrade` 位於遊戲內交互流，利用有序傳輸特性確保雙方確認狀態的正確序列。

---
*本文件由分析任務自動生成預定留檔。*
