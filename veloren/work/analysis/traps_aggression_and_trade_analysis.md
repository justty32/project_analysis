# 陷阱機制、AI 侵略性與交易協定深度分析 (Analysis)

## 1. 地城陷阱的物理與狀態聯動 (`common/systems/src/buff.rs`)
陷阱並非直接扣除生命值，而是透過 Buff 系統實現持續傷害。

### 核心代碼標註：
- **陷阱觸發 (`HaniwaTrap`)**：[Line 276] 偵測到踩踏後，系統執行「方塊替換」邏輯，將靜態陷阱變更為觸發態 `HaniwaTrapTriggered`。
- **狀態施加**：[Line 294] 陷阱產生的主傷害來源是 `BuffKind::Bleeding`。這解釋了為什麼某些高韌性 NPC 可以無視陷阱傷害（免疫流血）。
- **環境屬性自適應**：[Line 326] 高溫表面 (`HotSurface`) 直接與 `Burning` 狀態掛鉤，展示了地形方塊如何直接參與戰鬥數值運算。

## 2. AI 戰鬥侵略性與環境感知 (`server/agent/src/util.rs`)
Veloren 的 AI 具備基礎的戰術迴避能力。

### 關鍵機制：
- **血量驅動模式**：[Line 285] `handle_attack_aggression` 函數。AI 會根據血量在逃跑、保守與魯莽間切換，這是 Boss 多階段行為的底層基礎。
- **動態掩體尋找**：[Line 345] AI 在保守模式下會透過 `terrain.ray` 向側翼進行「探路」，尋找固體方塊 (`is_solid`) 來規避玩家的視線或遠程彈道。
- **動作預判**：[Line 375] AI 能偵測目標的 `is_melee_attack()` 狀態，從而觸發翻滾或格擋，提升了戰鬥的挑戰性。

## 3. 交易系統的數據一致性
### 協定設計：
- **鎖定機制**：交易封包包含雙方的 `ReducedInventory` 快照。任何一方修改交易物品都會導致快照失效，強制重置確認狀態。
- **網路流隔離**：交易數據透過 `in_game_stream` 傳輸，確保交易過程中的延遲不會影響同步的一致性判定。

---
*本文件由分析任務自動生成預定留檔。*
