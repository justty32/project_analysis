# 地理音效感知、載具物理與飛船導航深度分析 (Analysis)

## 1. 環境音訊的物理感知實作 (`voxygen/src/audio/ambience.rs`)
Voxygen 的音效系統具備精確的環境反饋。

### 核心代碼標註：
- **聲學環境濾波**：[Line 55] 透過 `set_ambience_master_filter` 實現跨介質音效轉換（水下 888Hz 悶響）。
- **高度與風場模型**：[Line 123] `alt_factor` 建立了風聲音量與海拔的線性關聯；[Line 128] `tree_factor` 則引入了植被對風速的物理屏蔽係數。
- **降雨動力學響應**：[Line 162] 雨聲音量隨攝像機垂直距離 (`cam_pos.z - terrain_alt`) 的平方反比衰減，模擬真實的聲場傳播。

## 2. 移動平台的實體吸附與速度補償
確保在高速載具上進行穩定的戰鬥與行走。

### 關鍵機制：
- **速度分量疊加**：當實體處於 `Mount` 狀態時，物理引擎會將宿主的 `Vel` 向量投影至乘員。這解決了傳統網路遊戲中常見的「載具移動、人留原地」的步調不一問題。
- **局部 Aabb 校驗**：載具體素被視為 `Dyna` (動態卷)，其內部的碰撞判定優先級高於世界地形，確保了船艙內部的封閉物理空間。

## 3. 飛船自動導航與路徑規劃 (`world/src/civ/airship_travel.rs`)
具備地理感知的宏觀導航系統。

### 核心技術：
- **航線地形掃描**：導航系統會定期採樣航線前方的 `alt_at` 高度圖。若前方地形隆起超過安全閾值，系統會發射修正指令，拉升飛船的 Z 軸目標值。
- **路點連通性網絡**：飛船並非隨機移動，而是遵循城鎮間的 `airship_route_map`。這與 `rtsim` 的經濟模型掛鉤，實現了商貿物資的實體化運輸。

---
*本文件由分析任務自動生成預定留檔。*
