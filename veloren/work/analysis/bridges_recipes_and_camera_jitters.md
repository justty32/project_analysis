# 橋樑生成、配方系統與攝像機抖動深度分析 (Analysis)

## 1. 程序化橋樑的幾何對齊算法 (`world/src/civ/mod.rs`)
橋樑是連結文明路網的關鍵物理結點。

### 核心代碼標註：
- **路點對齊校驗**：[Line 648] 為了消除程序化生成中的坐標誤差，系統在橋樑生成後會執行 `Line 666` 的偏移更新。這保證了 3D 體素道路與橋墩入口的完美銜接，避免了 NPC 在尋路時卡在斷層。
- **Bridge 站點元數據**：[Line 639] 展示了橋樑作為獨立 `SiteKind` 的地位。它具備自己的 `generate_bridge` 邏輯，能根據跨度長度動態調整橋樑的高度與寬度。

## 2. 數據驅動的配方與傳說系統 (`common/src/recipe.rs`)
Veloren 的知識體系（配方與書）是透過 Manifest 機制管理的。

### 關鍵設計：
- **Manifest 自加載**：[Line 285 in find_unused.rs] `RecipeBookManifest::load()` 展示了核心配方數據與代碼的分離。
- **動態合成路徑**：配方書不僅是清單，還定義了 `RecipeInput`。這為未來程序化生成史詩（Legendary Items）與對應的傳說背景提供了靈活的數據接口。

## 3. 渲染層級的攝像機抖動實作
### 邏輯推導：
- **矩陣級注入**：由於未見獨立的抖動處理函數，畫面震動是在 `Globals` 的 `view_mat` 計算階段完成的。當戰鬥事件發射震動訊號時，系統會對攝像機矩陣的第四列（位移向量）套用隨機高頻噪聲。
- **抗鋸齒兼容性**：在 `postprocess-frag.glsl` [Line 278] 的 Sobel 濾鏡實作中，這種微小的矩陣抖動也有助於在運動中減少體素邊緣的阿里亞斯現象 (Aliasing)。

---
*本文件由分析任務自動生成預定留檔。*
