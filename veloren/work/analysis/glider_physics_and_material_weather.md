# 滑翔物理、氣候材質與導航阻力深度分析 (Analysis)

## 1. 滑翔翼的空氣動力學模型 (`common/src/comp/fluid_dynamics.rs`)
Veloren 具備了類飛行模擬器的物理核心。

### 核心代碼標註：
- **失速物理模擬**：[Line 377] 實作了 $\approx 18^{\circ}$ 的失速閾值。超過此角度時，升力曲線發生非線性跌落，迫使玩家必須控制俯衝角度來維持空速。
- **生物阻力特化**：[Line 245-330] 根據現實生物動力學論文設定阻力係數。例如鳥類 (`BirdLarge`) 的 $C_d$ 為 0.2，而人類 (`Humanoid`) 為 0.7。這決定了不同物種在滑翔時的能量保持能力。
- **流體力學整合**：[Line 210] 統一了空氣與水的阻力公式，利用密度變量 $\rho$ 實現在不同介質間的物理無縫切換。

## 2. 環境光照與濕潤材質實作 (`terrain-frag.glsl`)
Shader 如何根據時間與氣候改變視覺材質。

### 關鍵機制：
- **動態曝光矩陣**：[Line 75] 定義了從 1.0 (白天) 到 10.0 (黑夜) 的曝光範圍。這配合 HDR Tone-mapping 實現了在極暗地城中也能看到微弱細節的效果。
- **氣候反射修正**：透過修改鏡面反射分量，當環境濕度（Rain）增加時，體素表面的 $f\_reflect$ 會被加權，模擬水漬覆蓋後的強反射與低粗糙度視覺感。

## 3. NPC 導航的動態代價計算
### 邏輯推導：
- A* 搜尋的 `transition_cost` 並非靜態常數。在 AI 決策層，系統會將「火堆」或「毒池」坐標標記為極高代價。
- **垂直跳躍代價**：當路點間存在 1 方塊以上的高度差時，導航器會額外增加約 2.0-5.0 的成本權重，促使 AI 優先選擇平坦路徑而非頻繁跳躍。

---
*本文件由分析任務自動生成預定留檔。*
