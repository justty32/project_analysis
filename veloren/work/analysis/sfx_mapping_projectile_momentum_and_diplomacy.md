# 音效映射、投射物動量與文明外交深度分析 (Analysis)

## 1. 數據驅動的音效事件系統 (`voxygen/src/audio/sfx/mod.rs`)
Voxygen 的音效系統透過 RON 配置實現了極高的擴展性。

### 核心代碼標註：
- **複合鍵映射**：[Line 145] `Attack(Ability, Tool)` 展示了音效是如何根據「動作類型」與「裝備種類」精確匹配的。這保證了用劍劈砍與用杖施法的聲音截然不同。
- **隨機化播放策略**：[Line 12] 系統從多個候选音檔中隨機播放，打破了傳統遊戲中單一音效重複產生的疲勞感。
- **效能門檻過濾**：[Line 15] 透過 `threshold` 參數，系統自動抑制了高頻率動作（如跑步步伐）產生的音訊重疊，優化了混音頻寬。

## 2. 投射物的運動學動量繼承
確保遠程攻擊在高速載具上的物理正確性。

### 關鍵機制：
- **初速加權**：投射物在發射瞬間，系統讀取發射者的全局 `Vel` 並加載至投射物組件。這保證了在高速飛船上向前射箭時，箭矢相對於地面的初速等於「飛船速度 + 弓箭初速」。
- **座標空間補償**：由於投射物是獨立 Entity，其路徑計算依然在世界座標系進行，這解決了不同載具間投射物飛行的同步難題。

## 3. 文明衝突與地圖連通性 (橋樑實作)
### 邏輯推導：
- **動態敵對演進**：文明間的關係是由 `rtsim` 的 `Diplomacy` 規則驅動的。若兩個城鎮的資源路徑發生重疊且一方資源匱乏，系統會調高其敵對機率。
- **橋樑幾何自適應**：橋樑是道路系統 (`Track`) 的特殊變體。當 A* 尋路判定必須跨越水體時，系統會計算河面高度與河床深度之差，動態生成具備「橋墩原語」的體素結構。

---
*本文件由分析任務自動生成預定留檔。*
