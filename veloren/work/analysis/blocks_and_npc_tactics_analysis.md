# 體素屬性、物理基礎與 NPC 戰術深度分析 (Analysis)

## 1. 高效體素架構 (`common/src/terrain/block.rs`)
Veloren 的地形系統在極小的內存佔用下實現了豐富的物理特性。

### 核心設計：
- **位遮罩優化 (Bitmasking)**：透過將 `BlockKind` 對齊到 16 位邊界，系統能以單次位運算判定「流體性」，極大加速了物理與網格渲染引擎。
- **混合數據字段**：`Block` 結構體根據 `is_filled` 狀態動態切換 24-bit 數據的用途（顏色 vs 精靈索引），實現了空間的高效複用。
- **物理特性映射**：方塊不僅是視覺單位，還承載了摩擦力 (`FRIC_GROUND`)、是否可爆炸以及流體密度等物理元數據。

## 2. NPC 戰鬥行為與戰術系統 (`server/agent/src/data.rs`)
AI 的行為並非單一邏輯，而是多層級的戰術組合。

### 關鍵機制：
- **戰術 (Tactic) 多樣性**：擁有超過 50 種專屬戰術，涵蓋了從普通野生動物到史詩 Boss。這確保了玩家面對不同敵人時需要不同的應對策略。
- **動態行為模式 (ActionMode)**：AI 會根據自身血量與環境壓力，在「魯莽」、「保守」與「逃跑」模式間切換。
- **目標威脅評估**：AI 能感知玩家所持武器的類型（遠程 vs 近戰），並據此調整切入角度與時機。

---
*本文件由分析任務自動生成預定留檔。*
