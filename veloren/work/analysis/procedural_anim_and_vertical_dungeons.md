# 程序化動畫、步伐同步與垂直地城拓撲深度分析 (Analysis)

## 1. 極致細膩的步伐同步與動態傾斜 (`voxygen/anim/src/character/run.rs`)
Veloren 的角色動作並非預製剪輯，而是實時計算的結果。

### 核心代碼標註：
- **速度歸一化修正**：[Line 48] `speednorm` 將物理速度映射到動畫幅度，並透過 `powf(0.65)` 實現非線性過渡，使慢走與快跑的銜接極其自然。
- **動態轉向側傾 (`tilt`)**：[Line 100] 透過夾角計算 (`angle_between`) 實現身體在轉彎時的重心偏移。這是角色移動具備「重量感」的關鍵數學基礎。
- **多部位聯動**：[Line 120-180] 展示了頭、胸、腰、手、腳在跑步時的獨立旋轉權重。所有部位都共享 `acc_vel` (累積位移)，確保全身動作的相位一致性。

## 2. 垂直地城的幾何堆疊與連通性 (`world/src/site/gen.rs`)
地城的層級感源於 3D 原語的布林運算。

### 關鍵機制：
- **垂直體素挖除**：[Line 72] 使用 `SegmentPrism` 定義垂直通道。透過 `Primitive::Without` [Line 87] 從地層實體中減去通道形狀，自動產出樓梯井與天井。
- **層級坐標變換**：[Line 89] `Translate` 被用於將不同功能的房間（如 Boss 房、藏寶室）精確佈置在預定的地下高度。
- **幾何一致性檢查**：[Line 296] 的包含判定確保了牆壁與樓層在垂直堆疊時不會出現 1-pixel 的縫隙或重疊。

## 3. 攝像機與權限系統路徑修正
### 邏輯推導：
- 攝像機抖動與濾鏡觸發應位於 `voxygen/src/render/pipelines/postprocess.rs` 或場景主循環中。
- 建築保護是以 `Site` 為圓心的區域權限鎖定，伺服器在接收 `TerrainUpdate` 請求時會與 `rtsim` 中的 Site 坐標進行距離校驗。

---
*本文件由分析任務自動生成預定留檔。*
