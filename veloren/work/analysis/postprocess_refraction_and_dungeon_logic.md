# 後處理視效、水下折射與地城連通深度分析 (Analysis)

## 1. 後處理著色器與動態衝擊 (`assets/voxygen/shaders/postprocess-frag.glsl`)
Veloren 透過全屏 Shader 營造環境感官。

### 核心代碼標註：
- **水下波動模擬**：[Line 265] 當偵測到攝像機處於 `MEDIUM_WATER` 時，利用時間種子對 UV 進行微小的正弦偏移，實現波光粼粼的視覺效果。
- **自適應色調映射**：[Line 75] `_illuminate` 函數。它根據環境光 (`max_light`) 與像素亮度進行非線性壓縮，確保了從深夜地城到烈日沙漠的視覺過渡。
- **邊緣強化 (Sobel)**：[Line 278] 實驗性的 Sobel 濾鏡被用於強化物體輪廓，這在 Boss 出場或發動大規模技能時能顯著提升畫面的力量感。

## 2. 物理正確的水下折射算法 (`skybox-frag.glsl`)
### 關鍵機制：
- **折射率常數**：[Line 53] 嚴格遵循物理數值 (1.3325)，確保了跨介質觀察時的視覺真實性。
- **頂點反折射修正**：[terrain-vert.glsl:133] 展示了如何透過「反轉折射向量」來校正被遮擋物體的位置，防止體素模型在水面交界處產生斷裂。

## 3. 地城房間連通性實作 (`world/src/site/plot/adlet.rs`)
### 核心邏輯：
- **異質空間融合**：強地 (Stronghold) 與自然洞穴 (Cavern) 的連接是透過 Aabr 邊界計算實現的。系統會確保兩者的邊界矩形有重疊區域，並在該區域強制生成「入口」Tile。
- **拓撲路徑保護**：透過 Site 元數據標註「關鍵路徑」方塊，防止程序化生成中的裝飾物（如石筍）阻塞地城的唯一出口。

---
*本文件由分析任務自動生成預定留檔。*
