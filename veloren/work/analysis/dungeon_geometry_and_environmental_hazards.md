# 地城幾何拓撲與環境危害深度分析 (Analysis)

## 1. 地城幾何樹與效能優化 (`world/src/site/gen.rs`)
Veloren 的地城生成基於一套強大的構造實體幾何 (CSG) 系統。

### 核心技術：
- **幾何原語樹 (`Painter`)**：[Line 767] 透過儲存 `Primitive` 的引用，系統構建了複雜的房間與通道網絡。
- **短路求值優化 (`order_by_depth`)**：[Line 810] 在進行體素包含判定時，系統會優先計算簡單的形狀，若坐標不在簡單形狀內則跳過複雜計算，極大提升了 3D 空間填充的效率。
- **自定義 Aabr 檢測**：[Line 296] 實作了精確的邊界包含算法，確保地城牆壁與樓層的體素對齊完美。

## 2. 環境壓力與溺水模擬 (`common/src/comp/health.rs`)
環境對實體生存的限制是透過流體介質與健康系統聯動實現的。

### 關鍵設計：
- **環境傷害歸因**：當實體處於 `Fluid::Liquid` 且深度超過閾值時，系統發射 `HealthChange` 事件。[Line 10]
- **傷害過濾**：溺水傷害被標記為非精確打擊 (`precise: false`) 與環境來源，這意味著它會繞過大多數角色的戰鬥閃避邏輯。

## 3. 種族文化地城生成 (`world/src/site/mod.rs`)
地城的外觀與佈局由其所屬種族決定。

### 核心流程：
- **`generate_adlet`**：[Line 847] 展示了犬人地城的生成入口。它結合了 `plot` 系統來規劃房間功能（如 Boss 房、寶箱房）。
- **`blit_aabr`**：[Line 839] 透過將 3D 原語「烙印」到地形畫布上，實現了具有文化風格的建築（如哥布林木質工事與犬人冰雪堡壘）的快速生成。

---
*本文件由分析任務自動生成預定留檔。*
