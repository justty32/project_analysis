# 戰鬥修正、文明史模擬與氣候邊界深度分析 (Analysis)

## 1. 移動載具上的 Melee 命中校驗 (`common/systems/src/melee.rs`)
Veloren 透過局部物理空間解決了動態平台的戰鬥同步問題。

### 核心代碼標註：
- **幾何遮擋檢查**：[Line 195] `is_blocked_by_wall` 函數。它同時考慮了發起者與目標的圓柱體積，並針對載具的局部 Volume 進行射線掃描，防止在船艙內產生越位攻擊。
- **體型自適應判定**：[Line 115] `rad = body.max_radius() * scale`。系統將實體的物理縮放比例（Scale）注入命中公式，這確保了 Boss 級生物與小型實體具備一致且物理正確的打擊感。

## 2. 文明起源與地圖選址算法 (`world/src/civ/mod.rs`)
世界歷史的宏觀演進基於幾何約束。

### 關鍵機制：
- **初始密度公式**：[Line 30] `initial_civ_count` 確保了文明點密度隨地圖面積對數增長，避免了大型地圖過於空曠或小型地圖過於擁擠。
- **鄰近約束器 (`ProximitySpec`)**：[Line 105] 實作了文明擴張的「生存本能」。透過 `avoid` (避開敵對生物群落) 與 `be_near` (靠近水源/礦產)，系統在隨機性中模擬了文明發展的資源導向性。
- **站點生成博弈**：[Line 285] 透過 `attempt` 邏輯模擬了文明建立定居點的難度，這直接影響了最終地圖上城鎮的分佈密度。

## 3. 氣候數學模型與群系邊界判定 (`world/src/sim/mod.rs`)
生態群系的產生是溫濕度矩陣的終端映射。

### 核心代碼標註：
- **溫濕度 CDF 分佈**：[Line 2559] 透過 `cdf_irwin_hall` 將高度、地理位置與隨機性混合，形成了連貫的環境參數場。
- **硬編碼生態邊界**：[Line 2773-2774] 定義了泰加林 (Taiga) 與稀樹草原 (Savannah) 的生存空間矩陣。這種數值閾值設計保證了生物群系在 Shader 渲染與 AI 生成中的邏輯統一性。

---
*本文件由分析任務自動生成預定留檔。*
