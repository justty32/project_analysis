# 地城機關門、燈具光照與氣候模擬深度分析 (Analysis)

## 1. 種族化地城機關門系統 (`common/src/terrain/sprite/mod.rs`)
Veloren 的地城交互是透過特定的 Sprite 屬性實現的。

### 核心代碼標註：
- **門與鎖鑰枚舉**：[Line 400-417] 定義了從 `BoneKeyhole` 到 `MinotaurKeyhole` 的完整鎖體系。這確保了不同文明的地城具有一致的物理交互邏輯但具備不同的視覺風格。
- **動畫門實作 (`DropGate`)**：[Line 432] 透過區分門體與底座，系統支持垂直移動的機關門效果。
- **碰撞體切換**：當門開啟時，方塊的物理標記從 `is_solid` 變更為非固體，允許實體通行。

## 2. 動態路燈與 LightEnabled 標記 (`Lamp`)
高效的全局光源管理機制。

### 關鍵機制：
- **狀態位控制**：[Line 460] 燈具 Sprite 具備 `LightEnabled` 屬性。這允許系統在 `voxygen` 渲染端根據此標記決定是否發射點光源，而無需在伺服器端頻繁重構體素數據。
- **多樣化燈具**：支持從 `MesaLantern` 到 `AirshipWallLantern` 等 20 多種光源，每種都共享相同的邏輯標記。

## 3. 世界生成中的氣候與地理模擬
### 邏輯推導：
- 氣候參數（濕度與降雨）是地質模擬的副產物。
- **地形阻擋 (Rain Shadow)**：[world/src/sim/mod.rs] 模擬了水氣受海拔抬升冷凝的物理過程。高海拔山脈的背風坡會因為水氣攔截而產生低濕度數值，進而映射為 `Desert` (沙漠) 群系。
- **動態濕度傳遞**：濕度從水體擴散，其衰減係數與距離平方及局部植被密度呈正相關。

---
*本文件由分析任務自動生成預定留檔。*
