# 特效掛鉤、動畫混合與尋路權重深度分析 (Analysis)

## 1. 動作狀態驅動的特效發射 (`voxygen/src/scene/figure/mod.rs`)
客戶端如何精確捕捉戰鬥時機並發射特效。

### 核心代碼標註：
- **階段進度計算**：[Line 1653-1666] 系統將 `CharacterState` 的計時器轉化為 0.0 到 1.0 的 `progress`。
- **武器感知更新**：[Line 1675] 透過傳入 `tool_kind` 與 `hand_info` 給動畫更新器，確保特效（如劍氣或冰霧）的掛載位置與武器骨骼完全對齊。
- **狀態過濾器**：[Line 1683] 大量匹配戰鬥相關狀態，確保只有在真正的動作執行（Action 幀）時才觸發視覺衝擊。

## 2. 動畫混合與線性插值技術 (`voxygen/anim/src/lib.rs`)
實現流暢動作融合的數學基礎。

### 關鍵設計：
- **骨架 Lerp 實作**：[Line 60] `skeleton_impls!` 宏自動生成了對 `Skeleton` 的 `Lerp` 支持。這允許系統在兩套不同的動作骨架（如「跑步」與「揮擊」）之間進行任意比例的混合。
- **法線矩陣同步**：[Line 127] `make_bone` 函數保證了在骨骼旋轉插值時，對應的法線也會同步更新，解決了體素模型在動作過渡時的光影閃爍問題。
- **動態庫熱加載**：[Line 150] 支持透過動態庫 (`veloren-voxygen-anim`) 更新動畫邏輯，實現了邏輯與渲染的熱重載。

## 3. 尋路系統的動態權重與路徑修正 (`common/src/path.rs`)
NPC 如何智能地處理複雜的地形阻礙。

### 核心流程：
- **可通行性原子判定 (`fn walkable`)**：[Line 521] 結合體素屬性進行基礎過濾。
- **導航向量修正**：[Line 504] 針對陸行生物，系統會主動剝離導航向量的 Z 軸分量，確保 A* 路徑能穩定貼合地面，防止 NPC 在不平整地形上產生飄浮感。
- **停滯邏輯保護**：[Line 506] 當路徑暫時中斷時，AI 會執行「望向遠方」的空轉邏輯，直到下一個計算 Tick 重新找到路徑，避免了死循環。

---
*本文件由分析任務自動生成預定留檔。*
